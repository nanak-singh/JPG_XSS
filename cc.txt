package cloudconfig.hostname_check

# Set the default outcome to "pass".
default result = "pass"

# If the input data doesn't have a hostname or if it's empty, fail the check.
result = "fail" {
  # Adjust this path (input.server.hostname) to match your actual input JSON.
  not input.server
  or input.server.hostname == ""
}
